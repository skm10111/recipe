import { Component } from '@angular/core';


@Component({
    selector: 'app-header',
    templateUrl: './header.component.html'
})
export class HeaderComponenet {
    // @Output() fertureSelected = new EventEmitter<string>();
    // onSelect(feature: string) {
    //     this.fertureSelected.emit(feature);
    // }
}